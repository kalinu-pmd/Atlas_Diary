# MongoDB connection string
CONNECTION_URL=mongodb+srv://<user>:<password>@cluster.mongodb.net/atlas-diary?retryWrites=true&w=majority

# JWT secret — use a long, random, unguessable string in production
JWT_SECRET=your_super_secret_jwt_key_here

# Server port (optional, defaults to 5000)
PORT=5000
